# CoT Stability (Minimal Setup)

This is a minimal, offline-ready scaf

### 🔑 Setup API Keys

Create a `.env` file in the project root with your own API key(s):
For Google Gemini
```
GEMINI_API_KEY=your_gemini_key_here
```
Or for OpenAI
```
OPENAI_API_KEY=your_openai_key_here
```